# Per-tenant secrets. Copied to tenants/<slug>/.env by `ruby deploy.rb spawn
# <slug>` and filled in by hand. Each tenant has their own copy of this file
# with their own DeepSeek key, Telegram bot, persona name, timezone, and
# allowlist. Never commit.

# --- Tenant identity (auto-set by `spawn`; do not change after first deploy) ---
# Short slug (lowercase letters/digits/dashes). Becomes the docker container
# suffix (zeroclaw-hub-<slug>), the compose project name (zeroclaw-<slug>),
# and the directory name under tenants/.
USER_SLUG=

# Loopback port on the VM that this tenant's gateway binds to. Each tenant
# needs a unique port; `spawn` picks the next free one starting at 42617.
# `ruby deploy.rb tunnel <slug>` reads this to set up the SSH tunnel.
ZEROCLAW_GATEWAY_PORT=

# --- DeepSeek via its Anthropic-compatible endpoint ---
# Paste your DeepSeek API key from https://platform.deepseek.com/. The
# container runs a small Node proxy on 127.0.0.1:8089 that injects
# `thinking: {type: "disabled"}` into every request before forwarding to
# https://api.deepseek.com/anthropic, so deepseek-v4-flash stays on the
# cheapest non-thinking pricing and ZeroClaw never has to bookkeep
# thinking blocks across turns. ZeroClaw's default_provider points at the
# proxy, not directly at api.deepseek.com.
DEEPSEEK_API_KEY=

# Optional: override the default model. We default to deepseek-v4-flash
# ($0.14/M input cache miss, $0.28/M output as of 2026-04). Other valid
# values: deepseek-v4-pro (more capable, ~3x cost), deepseek-chat (legacy
# non-thinking alias, deprecated 2026/07/24), deepseek-reasoner (legacy
# thinking, deprecated 2026/07/24). The proxy disables thinking regardless
# of which model you pick.
# ZEROCLAW_DEFAULT_MODEL=deepseek-v4-flash

# --- Web search (Brave Search API) ---
# Grab a key at https://api.search.brave.com/app/keys. The free "Data for
# Search" tier gives $5/mo in credits (~1500-1700 queries) with no overage
# risk as long as "Override Monthly Spend Limit" stays unchecked. Without
# this key, the built-in web_search tool falls back to DuckDuckGo, which
# aggressively blocks data-center IPs (you'll get captcha HTML instead of
# results).
BRAVE_API_KEY=

# --- Persona ---
# The name this tenant's agent answers to. Personality (voice, style, user
# context) lives in init-deepseek.sh and is identical across tenants; only
# the name and timezone are env-driven so each tenant can have a distinct
# identity (Rebecca, Becca, Iris, ...).
AGENT_NAME=Rebecca

# Tenant's local IANA timezone. Baked into USER.md so the agent assumes this
# zone whenever the user gives times without one, and passes --tz <this>
# when scheduling cron jobs. Examples: Europe/Madrid, America/New_York,
# Asia/Kolkata, UTC.
USER_TIMEZONE=UTC

# --- Autonomy ---
# Controls how much the agent can do without asking. Values:
#   supervised (default) - medium/high-risk commands prompt for approval,
#                          all commands allowed but risk gates apply.
#   full                 - kitchen-sink unrestricted: all commands, no
#                          approval gates, no rate/cost caps, no forbidden
#                          paths. Only use inside a disposable sandbox
#                          (e.g. this Docker container).
#   read_only            - agent observes, does not act.
AUTONOMY_LEVEL=supervised

# --- Telegram ---
# Each tenant has their OWN bot. The tenant DMs @BotFather, creates a bot,
# and pastes the HTTP API token here. Sharing one bot across tenants would
# defeat the isolation.
TELEGRAM_BOT_TOKEN=

# Numeric Telegram user id of the single tenant. Allowlist-of-one keeps
# memory and crons private to one user. Pull the id from getUpdates after
# they DM the bot:
#   curl "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/getUpdates"
TELEGRAM_ALLOWED_USER_ID=

# --- Optional: Gemini CLI (bundled in the image) ---
# GEMINI_API_KEY=
